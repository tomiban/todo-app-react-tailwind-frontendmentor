import React from "react";

const Footer = () => {
    return (
        <footer className="capilize mt-12 text-center text-gray-400">
            Drag and drop to reorder list
        </footer>
    );
};

export default Footer;
